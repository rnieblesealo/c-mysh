Tested Requirements: 
  - [x] exit
  - [x] End-of-input (Ctrl+D)
  - [x] cd
  - [x] 1 pipe
  - [x] 2 pipes
  - [x] Empty input 
  - [x] Argument overflow
    - (All this is in past tense)
      - Results in double-free, exec failing to find file, and IOT instruction
      - Trim this at the input collection stage
  - [ ] Buffer overflow
    - fgets should try and make sense of what's left over in the buffer

Bugs:
  - [x] Empty input segfaults when trailing spaces and then newline 
    - Works fine when any other kind of char is trailing
  - [x] Bad file desc. when piping 2 (output still ok)
    - Doesn't happen when piping 3, just 2
    - Error says it's close's fault
    - Seems we're closing something that's already closed since output is ok?

Notes:
  - Valgrind reports mem. leaks which I assume are child procs, so I'm not doing anything about it
